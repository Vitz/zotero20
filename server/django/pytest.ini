[pytest]
DJANGO_SETTINGS_MODULE = zotero20.settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
addopts = -ra --strict-markers
markers =
    integration: tests against a running docker-compose stack (set ZOTERO20_INTEGRATION=1)
